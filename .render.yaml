services:
  - type: web
    name: moe-viewer
    env: docker
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PISTON_URL
        value: https://piston.mantic.markets
      - key: PUPPETEER_EXECUTABLE_PATH
        value: /usr/bin/chromium
